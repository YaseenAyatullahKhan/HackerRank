# HackerRank
Answers to all the Problems I do on HackerRank. Will be editing them to make them more optimised (in terms of time complexity) soon.

Problems in Java:
1. Give the sum of the number of (i,j) pairs where i < j and array[i] + array[j] is divisible by a given integer k. Th array is of integers with length n (also given).